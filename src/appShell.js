/**
 * @file appShell 操作相关
 * @author mj(zoumiaojiang@gmail.com)
 */


import fs from 'fs-extra';
import path from 'path';


/* eslint-disable fecs-use-method-definition */
export default {


    /**
     * 基于某个 template ，找出这个 template 下面所有的 appShell
     *
     * @param  {string} template   模版标示
     * @return {Array<Object>}     appShell列表
     */
    getAppShells: async function (template) {

        return [];
    }
};

